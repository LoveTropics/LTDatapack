{
  "enter_commands": [
    "build enable @s",
    "tag @s add building",
    "fly enable",
    "execute if entity @s[gamemode=adventure] run gamemode survival"
  ],
  "exit_commands": [
    "build disable @s",
    "tag @s remove building",
    "execute unless entity @s[gamemode=creative] unless entity @s[gamemode=spectator] run fly disable"
  ]
}
