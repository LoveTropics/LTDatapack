{
  "enter_commands": ["build enable @s", "tag @s add building", "fly enable"],
  "exit_commands": ["build disable @s", "tag @s remove building", "execute if entity @s[gamemode=survival] run fly disable"]
}
